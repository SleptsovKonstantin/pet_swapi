import React from 'react';
import {useDelector} from 'react-redux';

import styles from './FavoritePage.module.css'

const FavoritePage = () => {
  const storeDate = useSelector(state => state)
  return (
    <div>
      
    </div>
  );
}

export default FavoritePage;